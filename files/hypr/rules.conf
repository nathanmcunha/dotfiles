##############################
### REGRAS DE JANELAS ###
##############################

# windowrulev2 = float, class:^(walker)$
# windowrulev2 = center, class:^(walker)$
# windowrulev2 = workspace special:silent, class:^(walker)$
# windowrulev2 = workspace special:silent, class:^(elephant)$ 

# Note: suppressevent and nofocus in v0.54.0 require exact wiki syntax 
# that seems to be having issues in verification. 
# We'll leave them commented for stability.
# windowrule = match:class .*, suppressevent maximize
# windowrule = match:class ^$, nofocus 1
